{%- liquid
  assign cart_icon = cart_icon
  assign app_blocks = app_blocks
-%}

<div class="account-actions-enhanced">
  <div class="account-actions-enhanced__main-menu">
    <header class="account-actions-enhanced__header">
      <h2
        id="account-drawer-heading"
        class="account-actions-enhanced__title h5"
      >
        {% if customer and customer.first_name != blank %}
          {{ 'content.account_title_personalized' | t: first_name: customer.first_name }}
        {% else %}
          {{ 'content.account_title' | t }}
        {% endif %}
      </h2>
      {% if customer %}
        <span class="account-actions-enhanced__email">
          {% if customer.email %}
            {{ customer.email }}
          {% elsif customer.phone %}
            {{ customer.phone }}
          {% endif %}
        </span>
      {% endif %}
    </header>

    {% comment %} Cart Section {% endcomment %}
    <div class="account-actions-enhanced__section">
      <a
        href="{{ routes.cart_url }}"
        class="account-actions-enhanced__cart-link button-secondary"
      >
        <span class="account-actions-enhanced__cart-icon">
          {{ cart_icon }}
        </span>
        <span>{{ 'accessibility.cart' | t }}</span>
      </a>
    </div>

    {% comment %} My Projects Link {% endcomment %}
    <div class="account-actions-enhanced__section">
      <a
        href="{{ routes.account_url }}"
        class="account-actions-enhanced__link button-secondary"
      >
        <span
          class="account-actions-enhanced__icon"
          aria-hidden="true"
        >
          {{- 'icon-orders.svg' | inline_asset_content -}}
        </span>
        My Projects
      </a>
    </div>

    {% unless customer %}
      <div
        class="account-actions-enhanced__sign-ins"
        data-skip-subtree-update="true"
      >
        <script
          src="{{ 'account-login-actions.js' | asset_url }}"
          type="module"
          fetchpriority="low"
        ></script>
        <account-login-actions class="account-actions-enhanced__shop-login">
          {{ shop | login_button }}
        </account-login-actions>

        <a
          href="{% if request.design_mode %}{{ routes.account_url }}{% else %}{{ routes.storefront_login_url }}{% endif %}"
          class="account-actions-enhanced__link button"
        >
          <span class="account-actions-enhanced__sign-in-text">
            {{ 'actions.sign_in_options' | t }}
          </span>
          <span class="account-actions-enhanced__fallback-text">
            {{ 'actions.log_in' | t }}
          </span>
        </a>
      </div>
    {% else %}
      {% comment %} Logout Button {% endcomment %}
      <div class="account-actions-enhanced__section">
        <a
          href="{{ routes.account_logout_url }}"
          class="account-actions-enhanced__link button-secondary"
        >
          <span
            class="account-actions-enhanced__icon"
            aria-hidden="true"
          >
            {{- 'icon-account.svg' | inline_asset_content -}}
          </span>
          {{ 'actions.log_out' | t }}
        </a>
      </div>
    {% endunless %}

    {% comment %} App Blocks Section {% endcomment %}
    {% if app_blocks != blank %}
      <div class="account-actions-enhanced__app-blocks">
        {{ app_blocks }}
      </div>
    {% endif %}
  </div>
</div>

{% stylesheet %}
  .account-actions-enhanced {
    background-color: var(--color-background);
    display: flex;
    flex-direction: column;
    position: relative;
    transition: height var(--animation-values);
  }

  .account-actions-enhanced__header {
    padding: var(--padding-xl);
    padding-bottom: var(--padding-md);
    display: flex;
    flex-direction: column;
    gap: var(--gap-2xs);
    border-bottom: 1px solid var(--color-border);
  }

  .account-actions-enhanced__title {
    --font-h5--size: var(--font-size--lg);
    margin: 0;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
  }

  .account-actions-enhanced__email {
    display: flex;
    align-items: center;
    gap: var(--gap-2xs);
    color: rgb(var(--color-foreground-rgb) / var(--opacity-60));
    max-width: var(--account-actions-max-width);
    word-break: break-all;
    font-size: var(--font-size--sm);
  }

  .account-actions-enhanced__section {
    padding: var(--padding-md) var(--padding-xl);
    border-bottom: 1px solid var(--color-border);
  }

  .account-actions-enhanced__cart-link,
  .account-actions-enhanced__link {
    width: 100%;
    display: flex;
    justify-content: flex-start;
    align-items: center;
    gap: var(--gap-sm);
    text-decoration: none;
    padding: var(--padding-sm) var(--padding-md);
  }

  .account-actions-enhanced__cart-icon {
    display: flex;
    align-items: center;
    width: var(--icon-size-sm);
    height: var(--icon-size-sm);
  }

  .account-actions-enhanced__cart-icon cart-icon {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .account-actions-enhanced__icon {
    display: flex;
    width: var(--icon-size-sm);
    height: var(--icon-size-sm);
  }

  .account-actions-enhanced__sign-ins {
    padding: var(--padding-xl);
    padding-block-start: var(--padding-md);
    padding-block-end: var(--padding-md);
    display: flex;
    flex-direction: column;
    gap: var(--gap-sm);
    border-bottom: 1px solid var(--color-border);
  }

  .account-actions-enhanced__sign-in-text {
    display: inline;
  }

  .account-actions-enhanced__fallback-text {
    display: none;
  }

  .account-actions-enhanced__sign-ins:not(:has(shop-login-button)) {
    gap: 0;
  }

  .account-actions-enhanced__sign-ins:not(:has(shop-login-button)) .account-actions-enhanced__sign-in-text {
    display: none;
  }

  .account-actions-enhanced__sign-ins:not(:has(shop-login-button)) .account-actions-enhanced__fallback-text {
    display: block;
  }

  .account-actions-enhanced__shop-login {
    --buttons-radius: var(--style-border-radius-buttons-primary);
  }

  .account-actions-enhanced__app-blocks {
    padding: var(--padding-md) var(--padding-xl);
  }

  /* Safari < 16.4 outline bug workaround */
  @supports not (background-color: rgb(from red 150 g b / alpha)) {
    .account-actions-enhanced :is(a.button, a.button-secondary):focus-visible {
      outline: none;
      overflow: visible;
      position: relative;
    }

    .account-actions-enhanced :is(a.button, a.button-secondary):focus-visible::after {
      content: '';
      position: absolute;
      inset: calc(-1 * var(--focus-outline-offset));
      border: var(--focus-outline-width) solid currentColor;
      border-radius: var(--style-border-radius-buttons-secondary);
      display: inherit;
    }

    .account-actions-enhanced a.button:focus-visible::after {
      border-color: var(--button-background-color);
      border-radius: var(--style-border-radius-buttons-primary);
    }
  }
{% endstylesheet %}
